{
    "name": "asahamemory-root",
    "version": "1.0.0",
    "description": "Root package for AsahaMemory project orchestration",
    "scripts": {
        "postinstall": "npm install --prefix Fullstack",
        "install-all": "npm install --prefix Fullstack",
        "build": "npm run build --prefix Fullstack",
        "start": "npm start --prefix Fullstack",
        "dev": "npm run dev --prefix Fullstack",
        "prod": "npm run install-all && npm run build && npm start"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "canvas-confetti": "^1.9.4"
    }
}
